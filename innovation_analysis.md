# PCNN 可创新点分析

创建日期：2026-04-28

本文档基于当前复现项目中的 PCNN 实现，对后续可行的创新优化方向进行梳理，目标是帮助在原论文《A Perception CNN for Facial Expression Recognition》的基础上开展改进实验、论文写作和消融设计。

## 1. 当前复现模型的核心特点

结合当前代码实现，PCNN 的主要结构特点如下：

1. 使用多个 ResNet-18 分支分别提取全局和局部表情特征。
2. 局部区域采用固定比例裁剪方式得到，不是自适应定位。
3. 局部特征经过拼接、插值和变换后与全局特征融合。
4. 局部分支的分类结果采用直接相加方式融合。
5. 训练损失由主分支分类损失和局部分支分类损失加权组成。

## 2. 当前模型存在的主要局限

从创新优化角度看，当前实现仍存在以下几个比较明确的问题：

### 2.1 固定区域划分不够灵活

模型目前通过固定比例切分人脸区域，例如上半部分、左右脸颊、嘴部区域等。这种方式虽然简单，但存在明显问题：

1. 当人脸姿态变化较大时，固定区域可能无法准确对应关键表情器官。
2. 当输入图像存在尺度变化时，固定比例裁剪的稳定性较差。
3. 当存在遮挡、偏转、表情幅度微弱时，固定划分可能错过关键局部信息。

### 2.2 局部与全局融合方式较为简单

当前局部分支输出最终采用直接求和方式融合：

```python
heads = head2 + head3 + head4 + head6 + head7
```

这种融合方式实现简单，但表达能力有限：

1. 无法区分不同局部区域对不同表情类别的重要性。
2. 无法根据输入样本动态调整局部区域权重。
3. 无法充分建模局部特征与全局特征之间的依赖关系。

### 2.3 多分支结构参数冗余较大

当前模型中多个局部分支分别使用独立的 ResNet-18 特征提取器，这会带来几个问题：

1. 参数量较大。
2. 训练和推理开销较高。
3. 多个分支之间存在重复特征学习，效率不高。

### 2.4 训练策略仍有提升空间

当前训练损失主要是两个交叉熵损失加权，训练增强策略也较基础，因此还有以下优化空间：

1. 类别不平衡处理不足。
2. 对遮挡、噪声、姿态变化的鲁棒训练不够强。
3. 缺少更细致的监督约束，如特征一致性、区域注意力约束等。

## 3. 可行的创新方向

下面给出几条最值得考虑的创新路线。

## 3.1 创新方向一：固定区域划分改为自适应区域感知

### 核心思想

将原始 PCNN 中基于固定比例的局部区域裁剪，改为能够根据输入图像动态确定关键表情区域的位置和范围。

### 可选实现方式

1. 基于人脸关键点进行动态区域裁剪。
2. 利用注意力热图自动生成局部区域。
3. 将当前的空间变换模块扩展为多区域可学习定位模块。
4. 使用 soft mask 对眼部、嘴部、脸颊区域进行动态加权。

### 优势

1. 更适应姿态变化和尺度变化。
2. 更适合真实场景下的表情识别。
3. 与原论文“局部感知”的思路高度一致，创新逻辑自然。

### 论文中可表述为

从固定启发式局部分区，升级为自适应表情感知区域定位机制。

## 3.2 创新方向二：局部与全局融合从简单求和升级为动态交互融合

### 核心思想

将当前局部分支简单相加的融合方式，升级为更强的动态融合机制，使模型能够根据输入样本自动判断哪些区域更重要。

### 可选实现方式

1. 可学习权重融合。
2. 门控融合机制。
3. 通道注意力或空间注意力融合。
4. Cross-Attention 局部全局交互模块。
5. 图结构建模，将眼睛、嘴巴、脸颊视为节点进行关系建模。

### 优势

1. 融合能力更强。
2. 能表达不同局部区域的重要性差异。
3. 容易设计消融实验，对比简单求和、加权求和、注意力融合等方式。

### 论文中可表述为

构建局部-全局动态交互融合模块，以替代原始 PCNN 中的静态求和策略。

## 3.3 创新方向三：共享骨干网络以降低参数冗余

### 核心思想

将多个独立 ResNet-18 分支改为共享主干特征提取，再在高层或中层分出多个局部专家头，从而降低参数量和推理开销。

### 可选实现方式

1. 前几层共享，后几层分支。
2. 整体共享 backbone，再对不同区域做 ROI Pooling。
3. 使用单个全局特征图生成多个局部区域特征。
4. 更换为更轻量的主干网络，如 MobileNetV3、EfficientNet、ConvNeXt-Tiny 等。

### 优势

1. 明显减少参数量和 FLOPs。
2. 降低训练和推理成本。
3. 更容易突出“精度与效率兼顾”的创新点。

### 论文中可表述为

在保留局部感知能力的同时，引入共享骨干结构以减少重复计算。

## 3.4 创新方向四：损失函数与训练策略增强

### 核心思想

保留原始双分支分类损失框架，在此基础上加入更适合表情识别任务的优化目标和训练策略。

### 可选实现方式

1. 使用 Focal Loss 缓解难分类样本问题。
2. 使用 Label Smoothing 提升泛化能力。
3. 使用 Class-Balanced Loss 处理类别不平衡。
4. 加入一致性约束，使局部与全局预测更协调。
5. 引入对比学习损失，增强类内紧凑性与类间分离性。

### 数据增强方向

1. Mixup
2. CutMix
3. Random Erasing
4. Occlusion Augmentation
5. RandAugment 或 AutoAugment

### 优势

1. 实现成本相对较低。
2. 容易与结构改进结合。
3. 有利于提升真实场景下的鲁棒性。

## 4. 最推荐的三条创新组合路线

为了避免一次性改动过多导致实验难以控制，建议选择“一个主创新 + 一个辅助创新”的方案。

### 方案一：自适应区域感知 + 注意力融合

这是最贴近原始 PCNN 设计思想的一条升级路线。

特点：

1. 保留“感知局部区域”的核心思路。
2. 强化局部定位能力。
3. 强化局部与全局的信息融合能力。

适合写成：

`Adaptive Perception CNN for Facial Expression Recognition`

### 方案二：共享骨干 + 动态局部全局交互

这条路线适合同时强调识别性能和模型效率。

特点：

1. 减少模型冗余。
2. 提升融合表达能力。
3. 适合增加复杂度分析和推理速度分析。

适合写成：

`Efficient PCNN with Shared Backbone and Dynamic Local-Global Fusion`

### 方案三：局部区域增强 + 鲁棒训练策略

这条路线更偏向工程实用和鲁棒性提升，适合数据集存在遮挡、噪声、姿态变化时使用。

特点：

1. 模型结构改动可以相对温和。
2. 训练策略优化空间较大。
3. 容易在 RAF-DB、FERPlus 上体现收益。

## 5. 最建议优先落地的修改顺序

如果希望尽快做出一版能跑实验、能做消融、也比较像论文创新的版本，建议按下面顺序推进：

1. 先改局部分支融合方式。
2. 再改局部区域提取方式。
3. 最后再考虑轻量化或共享骨干。

原因如下：

1. 融合模块最容易实现。
2. 区域感知模块最能体现方法创新。
3. 轻量化更适合作为进一步增强项。

## 6. 推荐的主创新路线

综合创新性、可实现性和论文表达效果，最推荐的方案是：

### 主创新

固定区域划分改为自适应区域感知。

### 辅创新

局部与全局融合由简单求和改为注意力融合或门控融合。

### 附加增强

加入更适合 FER 的损失函数或鲁棒训练策略。

这条路线的优点是：

1. 和原论文主线高度一致。
2. 更容易形成完整的方法逻辑。
3. 实验设计清晰，适合做消融。
4. 相比单纯更换 backbone，更像真正的方法创新。

## 7. 可直接用于论文写作的创新点表达

可以将新方法概括为如下逻辑：

1. 原始 PCNN 使用固定人脸区域划分，难以适应姿态变化、尺度变化和遮挡场景。
2. 为此，设计自适应区域感知模块，动态定位高表情敏感区域。
3. 同时，引入局部-全局动态交互融合模块，提升多区域信息整合能力。
4. 此外，通过改进损失函数与训练策略，提高模型在复杂场景下的泛化能力和鲁棒性。

## 8. 后续实验建议

后续可以围绕以下几类实验展开：

1. 与原始 PCNN 的整体精度对比。
2. 不同融合方式的消融实验。
3. 固定区域与自适应区域的消融实验。
4. 不同损失函数的消融实验。
5. 参数量、FLOPs、推理时间对比实验。
6. 混淆矩阵和类别级别分析。

## 9. 总结

PCNN 本身已经具备“局部区域 + 全局结构 + 融合分类”的清晰框架，因此非常适合作为二次创新基础。当前最有价值的创新方向主要包括：

1. 自适应区域感知。
2. 动态局部全局融合。
3. 共享骨干与轻量化设计。
4. 损失函数与训练策略增强。

如果目标是做一版最稳妥、最像论文方法创新、又相对容易落地的改进版，建议优先采用：

自适应区域感知 + 注意力融合 + 适度训练策略增强。
